---
# Kanata role handlers

- name: Reload kanata LaunchDaemon
  ansible.builtin.shell: |
    sudo launchctl unload {{ kanata_launchd_plist }} 2>/dev/null || true
    sudo launchctl load -w {{ kanata_launchd_plist }}
  listen: "Reload kanata LaunchDaemon"
